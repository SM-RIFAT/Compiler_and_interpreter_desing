int address = idcheck($);
if(address != -1)
{
    gen_code();
}


gen_label()


LD_INT, 
LD_VAR, 
STORE, 
SCAN_INT_VALUE, 
PRINT_INT_VALUE, 
ADD,
SUB,
GT_OP,
LT_OP,
IF_START,
ELSE_START,
ELSE_END,
WHILE_LABEL,
WHILE_START,
WHILE_END